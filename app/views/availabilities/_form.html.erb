<%= form_for(@availability) do |f| %>
  <% if @availability.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@availability.errors.count, "error") %> prohibited this availability from being saved:</h2>

      <ul>
      <% @availability.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :day %><br />
    <%= f.select(:day, [['Sunday', 0], ['Monday', 1], ['Tuesday', 2], ['Wednesday', 3], ['Thursday', 4], ['Friday', 5], ['Saturday', 6] ] ) %>
  </div>
  <div class="field">
    <%= f.label :begin %><br />
    
    <%= select(:begin, :day, [['Sunday', 1], ['Monday', 2], ['Tuesday', 3], ['Wednesday', 4], ['Thursday', 5], ['Friday', 6], ['Saturday', 7]])%>
    <%= f.time_select(:begin, {:ampm => true }) %>
  </div>
  <div class="field">
    <%= f.label :end %><br />
    <%= select(:end, :day, [['Sunday', 1], ['Monday', 2], ['Tuesday', 3], ['Wednesday', 4], ['Thursday', 5], ['Friday', 6], ['Saturday', 7]])%>
    <%= f.time_select(:end, {:ampm => true }) %>    					
  </div>
  <div class="field">
    <%= f.label :end %><br />
    					
  </div>
  <div class="actions">
    <%= f.submit :class => "action" %>
  </div>
<% end %>
