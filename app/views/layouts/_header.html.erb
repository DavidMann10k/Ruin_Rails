<header>
	<table>
		<tr>
			<td id="logo"><h1><%= link_to logo, root_path, :id => "logo" %></h1></td>
			<td id="site">
				<span><%= link_to "About", about_path %></span>
				<span><%= link_to "Forums", divisions_path %></span>
				<span><%= link_to "Users", users_path %></td></span>
			</td>
			<td id="account">
				<span><% if signed_in? %>
					<%= current_user.email %>
					<%= link_to "Profile", :action => "show", :controller => "users", :id => current_user.id %>
					<%= link_to "Sign out", "/signout" %>
				<% else %>
					<%= link_to "Sign In / Register", "/authenticate" %>
				<% end %></span>
			</td>
		</tr>
	</table>
</header>