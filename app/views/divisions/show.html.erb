<%= link_to "/Divisions", divisions_path %>
<div class="list">
	<h3 class="title">
		<sup><%= @division.id %> <%= @division.dex %></sup>
		<%= @division.title %>			
		<sup><%= link_to "X", @division, 	:method => :delete,
						  	  				:confirm => "Division and all child forums will be permanently and irrecoverably destroyed!  Are you sure?",
						  	  				:title => "Delete #{@division.title}",
						  	  				:class => "destroy" %></sup>
		<sup><%= link_to "Edit", edit_division_path(@division.id), :class => "edit" %></sup>
	</h3>
		
	<table>
		<% @division.forums.each do |f| %>
			<tr class="item">
				<td class="thin"><%= f.id %></td>
				<td class="thin"><%= f.dex %></td>
				<td><%= link_to f.title, :action => "show", :controller => "forums", :id => f.id %></td>
			</tr>
		<% end %>
	</table>
	<%= link_to "+Forum", new_forum_path(:division_id => @division.id), :class => "add_item" %>
</div>